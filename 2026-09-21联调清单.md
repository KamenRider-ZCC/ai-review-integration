# 2026-09-21 AI评标前后端联调清单

> 目标：关闭前端Mock，使用真实HTTP接口跑通AI评审进度、结果查看、反馈保存和文件预览完整流程。

## 一、联调前先向后端确认

- [ ] AI评标后端联调地址（只记录服务地址，不记录Token）
- [ ] 一个有效测试Token和一个无效Token
- [ ] 一个可访问的`reviewId`
- [ ] 一个处于解析失败且`retryable=true`的`fileId`
- [ ] 一组形式评审坐标：`reviewItemId + bidderId`
- [ ] 一组详细评审坐标：`reviewItemId + bidderId`
- [ ] 至少一条AI与专家结果不一致的数据
- [ ] 至少一条带引用材料的结果：`fileId + evidenceId`
- [ ] 如需验证全部不一致项，至少准备两名专家的数据，并包含本人可编辑、他人只读记录

测试坐标记录：

| 参数 | 测试值 |
| --- | --- |
| 后端地址 |  |
| `reviewId` |  |
| 失败文件`fileId` |  |
| 形式评审`reviewItemId` |  |
| 形式评审`bidderId` |  |
| 详细评审`reviewItemId` |  |
| 详细评审`bidderId` |  |
| 预览`fileId` |  |
| 预览`evidenceId` |  |

> 不要把完整`Access-Token`、预览地址写进本文档、聊天记录、截图或问题单。

## 二、切换真实后端

在工程目录执行：

```powershell
cd D:\projects\demo\ai-review-integration
Copy-Item .env.example .env.local
```

将`.env.local`调整为：

```dotenv
VITE_USE_MOCK=false
VITE_AI_REVIEW_API_BASE=/ai-review-api/v1
VITE_API_PROXY_TARGET=https://后端联调地址
VITE_ALLOWED_PARENT_ORIGINS=http://127.0.0.1:4176,http://localhost:4176
```

- [ ] 修改环境变量后重新执行`pnpm dev`，不能只刷新浏览器
- [ ] 打开`http://127.0.0.1:4176/ecp-simulator.html`
- [ ] 将后端提供的测试Token设置为当前域名下的`Access-Token` Cookie
- [ ] 确认Token按原值发送，请求头中没有`Bearer`前缀
- [ ] 不在页面URL中传Token、专家账号、专家姓名或专家结果
- [ ] 不发送任何`X-Test-*`请求头

本地模拟页会写入`LOCAL-DEMO-TOKEN`作为默认值。真实联调时应在页面加载后，通过浏览器Cookie工具将其覆盖为后端提供的测试Token；覆盖后不要刷新宿主模拟页。

## 三、先验证公共通信规则

任选一个GET接口，在浏览器Network中确认：

- [ ] 请求路径以`/ai-review-api/v1`开头，未重复或缺少路径前缀
- [ ] `Accept: application/json`
- [ ] 请求头包含`Access-Token`，值与测试Token一致
- [ ] 请求头包含非空且每次请求唯一的`X-Request-Id`
- [ ] POST、PUT存在JSON请求体时包含`Content-Type: application/json`
- [ ] 后端可返回HTTP 200和统一响应包络：`success、code、message、requestId、data`
- [ ] 前端错误提示中的`requestId`能与后端日志对应
- [ ] 浏览器控制台没有CORS、脚本或Vue运行时错误

## 四、六个接口按顺序联调

### 1. AI评审进度

`GET /reviews/{reviewId}/progress`

- [ ] 进入“打开AI评审进度”后立即发起请求
- [ ] 文件解析、初步评审、详细评审状态与后端数据一致
- [ ] `pollingRequired=true`时按`pollingIntervalSeconds`继续轮询
- [ ] `pollingRequired=false`时停止轮询
- [ ] 仅失败且`retryable=true`的文件显示“重新解析”
- [ ] 失败原因、进度数值、完成数量显示正确

### 2. 失败文件重新解析

`POST /reviews/{reviewId}/files/{fileId}/retry`

- [ ] 请求体为`{}`，只操作当前失败文件
- [ ] 成功时返回`accepted=true`和`retryTaskId`
- [ ] 成功后恢复进度轮询，界面状态随进度接口更新
- [ ] 重复点击不会创建重复任务，能处理`FILE_RETRY_IN_PROGRESS`
- [ ] 非失败文件或不可重试文件不会在前端出现重试入口

### 3. 当前评审项AI结果

`GET /reviews/{reviewId}/result`

形式评审AI入口：

- [ ] 参数为`FORMAL_REVIEW + reviewItemId + bidderId + action=AI`
- [ ] `resultType=DECISION`
- [ ] 显示AI判定、专家判定、AI评审总结和引用材料
- [ ] `showAiDecisionFeedback=false`，不显示反馈区

详细评审反馈入口：

- [ ] 参数为`DETAILED_REVIEW + reviewItemId + bidderId + action=AI_FEEDBACK`
- [ ] `resultType=SCORE`，`aiScore、expertScore、maxScore`保持字符串格式
- [ ] 显示主观项或客观项、AI评分总结和专家分数
- [ ] 后端确认不一致时`showAiDecisionFeedback=true`
- [ ] 实际不存在差异时，即使`action=AI_FEEDBACK`也不显示反馈区
- [ ] 上一项、下一项直接使用后端返回的矩阵坐标，前端不自行拼接

### 4. 全部当前专家不一致项

`GET /reviews/{reviewId}/differences`

- [ ] 使用无矩阵坐标入口，只传`reviewId + action=AI_FEEDBACK`
- [ ] 首次请求不传`cursor`，默认定位第一个未完成反馈项
- [ ] 显示全部当前专家的不一致项和反馈完成数量
- [ ] 当前专家本人记录`feedbackEditable=true`，其他专家记录只读
- [ ] 上一项、下一项使用后端返回的`previousCursor、nextCursor`
- [ ] 无不一致项时`items=[]、totalCount=0、currentItem=null`并显示空状态
- [ ] 游标过期或列表版本变化时能够显示后端业务错误

### 5. AI判定反馈保存

`PUT /reviews/{reviewId}/feedback`

- [ ] 请求体只包含`reviewStage、reviewItemId、bidderId、problemTypes、problemDescription`
- [ ] 不上传专家账号、专家姓名、专家结果或完整矩阵
- [ ] 至少选择一个问题类型后保存按钮才可用
- [ ] 选择“其他”时问题说明必填，最多200字
- [ ] 保存成功后重新查询服务端数据并正确回显
- [ ] 重复保存覆盖本人原反馈，不影响其他专家
- [ ] 全部不一致项的已完成数量在保存后同步更新
- [ ] 验证`PROBLEM_DESCRIPTION_REQUIRED`、无差异不可保存等业务错误

### 6. 投标文件原文预览

`GET /reviews/{reviewId}/files/{fileId}/preview?evidenceId={evidenceId}`

- [ ] 从AI评审总结点击“查看投标文件原文”后发起请求
- [ ] 返回短时`previewUrl`、当前位置、页码和引用内容
- [ ] 文件名、引用位置和正文预览正确
- [ ] 上一处、下一处使用后端返回的`fileId + evidenceId`重新请求
- [ ] 第一处禁用“上一处”，最后一处禁用“下一处”
- [ ] 预览地址失效、文件不支持预览、引用与文件不匹配时提示正确

## 五、必须补测的异常场景

- [ ] Cookie中无`Access-Token`：前端不请求后端并提示登录态缺失
- [ ] 无效Token：HTTP 200、`code=TOKEN_INVALID`
- [ ] 当前账号无项目权限：`code=REVIEW_ACCESS_FORBIDDEN`
- [ ] 一体化平台依赖不可用：`code=DEPENDENCY_UNAVAILABLE`
- [ ] 后端服务不可达或超时：页面显示网络错误或超时提示
- [ ] 响应缺少`success`或成功响应`data=null`：前端提示响应不符合契约
- [ ] URL缺少`reviewId`或矩阵坐标只传一部分：前端在请求前拦截
- [ ] AI状态为`AI_REVIEWING`：结果和总结为空，显示处理中状态
- [ ] 专家结果为空：不判定为不一致，不显示反馈区

## 六、常见问题快速定位

| 现象 | 优先检查 |
| --- | --- |
| 浏览器请求404 | `VITE_AI_REVIEW_API_BASE`是否重复或缺少`/ai-review-api/v1` |
| 请求没有到后端 | `.env.local`是否生效、是否重启Vite、代理目标是否可访问 |
| 浏览器提示CORS | 后端允许来源和允许请求头中是否包含实际前端Origin与`Access-Token` |
| 页面提示Token缺失 | 当前页面域名下是否存在可由JavaScript读取的`Access-Token` Cookie |
| 后端提示Token无效 | Token是否过期、是否误加`Bearer`、是否被模拟页默认Token覆盖 |
| 返回成功但页面报契约错误 | `success、code、message、requestId、data`是否齐全，成功时`data`是否为`null` |
| 页面数据与后端不一致 | 用页面错误中的`requestId`查后端日志，不要依赖截图猜测 |
| 原文预览空白 | `previewUrl`是否可访问、是否过期、响应头是否允许iframe加载 |

## 七、问题记录模板

| 时间 | 场景/接口 | 请求参数摘要 | 前端现象 | `code` | `requestId` | 前端结论 | 后端结论 | 负责人/状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |

记录要求：

- 保留`requestId`、业务响应码和必要的非敏感参数；
- 不记录完整Token、完整预览地址或投标文件敏感内容；
- 先区分入口参数错误、网络/代理问题、接口契约问题和业务数据问题；
- 明确由前端、AI评标后端、一体化平台还是ECP数据负责处理。

## 八、联调完成标准

- [ ] 六个接口至少各成功调用一次
- [ ] 进度轮询、失败重试、判定结果、评分结果、反馈保存、全部不一致项和原文预览均跑通
- [ ] Token、请求ID、代理转发和统一响应包络符合契约
- [ ] 关键异常有明确页面提示，且能通过`requestId`定位后端日志
- [ ] 浏览器控制台无未处理错误
- [ ] `pnpm typecheck`、`pnpm lint`、`pnpm build`全部通过
- [ ] 所有遗留问题已记录负责人、优先级和下一步，不以口头结论代替问题单

