# 2026-09-23 AI评标迁入前客户问题清单

> 23号只拉取并阅读客户“一体化”前端代码，不迁入AI评标代码。AI评标前端先完成公司内部后端联调，再迁入一体化系统；ECP系统由客户开发人员接入Loader并负责提供入口参数。

## 一、目前已经确定，不再重复询问

- AI评标业务页面迁入客户“一体化”前端工程，不迁入ECP工程。
- 评标抽屉由AI页面自己创建，ECP不负责开发抽屉内容。
- ECP宿主侧通过`ai-review-loader.js`创建iframe并打开AI页面。
- 登录状态下，接口请求头使用原始`Access-Token`，不添加`Bearer`前缀。
- AI结果、专家结果、Token和文件内容不通过URL或`postMessage`传递。
- 六个AI评标接口先在公司内完成真实后端联调，23号不向客户验证业务接口数据。
- UI以《初评打分一体化1.html》为准，接口业务以《AI评标前后端接口设计说明书》为准。

## 二、23号必须确认的问题

以下问题会直接决定后续代码怎么迁，最好当面得到明确答案、负责人和示例代码。

### 时间有限时优先确认的10项

1. 正确仓库、目标分支、开发权限以及本地启动/构建方式。
2. AI模块目标目录、空白Layout和两个iframe路由的最终地址。
3. ECP iframe内能否实际取得一体化域下的`Access-Token`，Cookie属性是什么。
4. 必须复用的公共Axios实例，以及Token、响应拆包和登录失效的处理方式。
5. AI接口网关前缀、环境配置位置、开发代理和生产转发负责人。
6. 必须替换的CNNC公共组件范围，以及是否有可用的公共文件预览组件。
7. ECP侧Loader接入负责人、脚本托管位置和各环境`appBaseUrl`。
8. ECP四类入口的准确位置，以及四个业务ID/枚举的字段映射。
9. ECP与一体化各环境Origin，以及iframe、CSP、X-Frame-Options的配置负责人。
10. 内部后端联调完成后的迁入、代码评审、联合验证和上线流程。

### 1. 一体化代码和开发方式

- [ ] **应该拉取哪个仓库、分支或Tag？后续AI评标代码提交到哪个目标分支？**
  - 记录：仓库__________　当前分支__________　目标分支__________
- [ ] **我是否有拉取、安装依赖、推送分支和提交合并请求的权限？代码评审由谁负责？**
  - 记录：权限情况__________　评审人__________
- [ ] **本地启动、类型检查、Lint、构建和测试分别执行什么命令？是否需要内部npm源、VPN或环境配置文件？**
  - 记录：启动__________　检查/构建__________　内部依赖要求__________
- [ ] **AI评标模块应放在哪个业务目录？客户是否有现成的同类模块可以作为目录、命名和写法范例？**
  - 记录：目标目录__________　参考模块__________

### 2. 页面路由和加载方式

- [ ] **AI页面是否可以提供两个“无一体化头部、菜单和面包屑”的独立iframe路由？应复用哪个空白Layout？**
  - 原因：如果套用一体化常规Layout，抽屉只会覆盖内容区，不能覆盖整个iframe视口。
  - 记录：空白Layout__________　参考路由__________
- [ ] **两个页面最终使用什么路由前缀？是否需要动态路由、登录守卫、菜单权限或按钮权限？**
  - 建议路由：进度页`/ai-review/progress`，评标页`/ai-review/page`。
  - 记录：最终路由__________　权限配置__________
- [ ] **一体化前端部署时使用什么`base/publicPath`和history回退规则？直接刷新上述iframe地址能否正常打开？**
  - 记录：部署基路径__________　history配置负责人__________
- [ ] **AI页面只从ECP iframe打开，还是还要支持一体化系统内直接打开？**
  - 记录：仅iframe / 还需直接打开__________

### 3. 登录态和请求层

- [ ] **ECP嵌入一体化页面后，一体化页面域名下是否一定已有`Access-Token` Cookie？请现场用浏览器确认Cookie的Domain、Path、SameSite、Secure和HttpOnly。**
  - 原因：打开ECP不等于跨域iframe一定能读取一体化域的Cookie；这是迁入前最需要确认的一项。
  - 记录：ECP Origin__________　一体化 Origin__________
  - 记录：Domain__________　Path__________　SameSite__________　Secure__________　HttpOnly__________
- [ ] **如果`Access-Token`为HttpOnly或iframe内不可读，一体化公共Axios实例是否会自动携带或注入Token？应复用哪段现有代码？**
  - 记录：公共请求文件__________　Token处理位置__________
- [ ] **公共Axios实例的正式导入路径和调用方式是什么？它是否已经完成统一响应拆包和业务错误处理？**
  - 需要核对：`success、code、message、requestId、data`是否仍能完整取得。
  - 记录：导入路径__________　返回值形态__________　错误处理方式__________
- [ ] **公共请求层会不会自动添加`Bearer`、覆盖`Access-Token`或删除`X-Request-Id`？自定义`X-Request-Id`是否允许通过网关？**
  - 记录：Token实际请求头__________　请求ID规则__________
- [ ] **Token失效时平台的统一行为是什么：只提示、跳登录页、刷新Token，还是由拦截器处理？AI页面应该调用哪个公共方法？**
  - 记录：处理方式__________　公共方法/示例__________
- [ ] **`localStorage.ei_UserInfo`在ECP iframe场景下是否可用？AI评标是否被要求接入用户姓名、账号或部门信息？**
  - 说明：当前接口设计由后端根据Token识别专家，前端不主动上传这些用户信息。
  - 记录：是否需要__________　用途__________

### 4. 接口地址和环境配置

- [ ] **一体化工程的开发、测试和生产环境变量分别放在哪里，命名规范是什么？**
  - 需要配置：AI后端基础路径、允许的ECP来源、必要时的开发代理地址。
  - 记录：配置文件/配置中心__________　负责人__________
- [ ] **浏览器最终通过什么网关前缀访问AI评标后端？是否继续使用`/ai-review-api/v1`？由谁配置开发代理和生产网关转发？**
  - 记录：前缀__________　开发代理负责人__________　生产网关负责人__________
- [ ] **网关/CORS是否允许`Access-Token`和`X-Request-Id`请求头？一体化和AI后端跨域时是否需要`withCredentials`？**
  - 记录：允许请求头__________　跨域/凭证规则__________
- [ ] **平台有没有统一的接口超时、重试和错误提示规范？20秒超时是否可以沿用？**
  - 记录：超时__________　重试__________　错误提示规范__________

### 5. 公共组件和样式

- [ ] **CNNC公共组件库的实际包名、版本、文档和示例代码在哪里？哪些组件必须使用公共封装，哪些可以直接使用Ant Design Vue？**
  - 重点组件：Drawer、Button、Checkbox、Alert、Spin、Empty、Message、Icon。
  - 记录：包名/版本__________　文档__________　强制替换范围__________
- [ ] **公共抽屉是否支持560px宽度、遮罩、正文滚动、自定义头部和固定底栏？在iframe中使用时是否有统一写法？**
  - 记录：组件/示例__________　不支持的能力__________
- [ ] **一体化是否已有公共PDF/文件预览组件？它能否接收后端返回的短时`previewUrl`，并支持指定页码或引用位置？**
  - 记录：组件路径__________　Props/API__________　支持格式__________
- [ ] **弹窗、消息和预览组件是否使用Teleport？挂载到`body`还是业务容器？平台的z-index规范是什么？**
  - 记录：挂载规则__________　z-index范围__________
- [ ] **主题色、字号、间距和暗色/换肤能力从哪里获取？AI页面局部样式是否需要CSS Modules、Scoped或统一命名前缀？**
  - 记录：主题入口__________　样式隔离要求__________
- [ ] **项目是否要求国际化、埋点、前端异常上报、权限指令或无障碍规范？AI模块分别接入什么公共能力？**
  - 记录：必接能力__________　参考模块__________

### 6. ECP和Loader接入

- [ ] **ECP侧由谁负责接入`ai-review-loader.js`？请确定一名可以对接入口参数和页面生命周期的开发人员。**
  - 记录：负责人__________　联系方式__________
- [ ] **Loader脚本放在ECP工程、一体化静态资源还是统一CDN？如何做版本更新和缓存失效？**
  - 记录：托管位置__________　引用地址__________　版本策略__________
- [ ] **各环境的ECP Origin和一体化AI页面`appBaseUrl`分别是什么？**
  - 记录：开发__________　测试__________　生产__________
- [ ] **ECP是否允许引入一个暴露`window.AIReview`的普通JavaScript文件？全局名称是否冲突，是否有统一脚本加载规范？**
  - 记录：允许方式__________　全局对象要求__________
- [ ] **以下四类入口在ECP的准确位置、触发时机和参数来源是什么？**
  - AI评审进度：`page=progress + reviewId`
  - 形式评审AI：`page=page + reviewId + action=AI + 完整矩阵坐标`
  - 详细评审反馈：`page=page + reviewId + action=AI_FEEDBACK + 完整矩阵坐标`
  - 全部专家不一致项：`page=page + reviewId + action=AI_FEEDBACK`，不传矩阵坐标
  - 记录：入口/参数映射__________
- [ ] **ECP现有字段如何映射为`reviewId、reviewStage、reviewItemId、bidderId`？`reviewStage`能否稳定提供接口约定枚举？**
  - 记录：字段映射__________　转换负责人__________
- [ ] **ECP路由切换、关闭页签或重复打开AI页面时，应由谁调用`AIReview.close/destroy`？同一页面是否可能同时打开多个AI窗口？**
  - 记录：生命周期约定__________　是否多实例__________
- [ ] **ECP是否允许Loader锁定宿主滚动，并创建最高层级的全屏iframe？进度浮窗的默认宽度和z-index是否会与ECP组件冲突？**
  - 记录：滚动处理__________　z-index__________　宽度限制__________

### 7. iframe和安全策略

- [ ] **一体化服务器是否允许页面被ECP嵌入？`Content-Security-Policy frame-ancestors`和`X-Frame-Options`由谁配置？**
  - 记录：允许来源__________　配置负责人__________
- [ ] **一体化页面的CSP是否允许加载AI接口、Loader通信以及后端返回的文件预览地址？**
  - 重点策略：`connect-src、frame-src、script-src`。
  - 记录：现有策略__________　调整负责人__________
- [ ] **文件预览服务是否允许在一体化iframe内部再次用iframe打开？是否存在跨域、下载头或防嵌套限制？**
  - 记录：允许方式__________　不支持时的公共预览方案__________
- [ ] **生产环境允许的ECP来源是否固定？是否有多个域名、端口或统一门户需要加入白名单？**
  - 记录：Origin白名单__________

### 8. 合并和验收

- [ ] **完成公司内部后端联调后，客户希望按什么流程迁入：开发分支、联调环境、代码评审、测试、上线？**
  - 记录：流程__________　各阶段负责人__________
- [ ] **迁入后必须执行哪些质量门禁？是否要求单元测试、覆盖率、Sonar、依赖扫描或浏览器兼容测试？**
  - 记录：质量门禁__________
- [ ] **客户验收由谁负责，验收环境如何进入？ECP接入和一体化迁入是否可以在同一环境联合验证？**
  - 记录：验收人__________　环境__________　联合验证方式__________

## 三、拉取代码后先自己核对，不必一上来就问

以下内容通常可以从工程中直接找到。23号先定位文件和示例；找不到或存在两套写法时，再向客户确认哪套是正式规范。

- [ ] `package.json`和锁文件：Vue、Vite、TypeScript、Ant Design Vue、Pinia、Router、Axios的实际版本及包管理器。
- [ ] Node版本文件和README：Node版本、本地启动及构建命令。
- [ ] `src`目录：业务模块、公共组件、API、类型、工具、样式的组织方式。
- [ ] Router：路由注册、空白Layout、权限守卫、动态路由、404和history模式。
- [ ] 请求层：Axios实例、Token注入、响应拆包、错误处理、超时和请求ID。
- [ ] 环境配置：开发代理、测试/生产网关、`base/publicPath`和环境变量命名。
- [ ] 公共组件：Drawer、Message、Preview等组件的真实调用示例。
- [ ] 主题和样式：设计Token、全局样式、Scoped/CSS Modules、z-index和Teleport规则。
- [ ] 状态和用户信息：Pinia模块、`ei_UserInfo`读取及登录失效处理。
- [ ] 代码规范：路径别名、命名、ESLint、Prettier、提交规范和CI脚本。

建议当天记录这些“参考文件”，后续迁入时直接照着写：

| 事项 | 客户工程参考文件 |
| --- | --- |
| 空白iframe路由 |  |
| Router注册和权限 |  |
| Axios公共实例 |  |
| Token和登录失效 |  |
| 统一响应/错误处理 |  |
| 公共抽屉 |  |
| 公共文件预览 |  |
| 主题和样式 |  |
| 环境变量/代理 |  |
| 同类业务模块 |  |

## 四、23号暂时不需要问后端业务细节

以下内容属于公司内部AI后端联调，应按《2026-09-21联调清单》验证，不要和客户一体化前端问题混在一起：

- 六个接口的真实响应字段和测试数据；
- 进度轮询、失败重试、反馈保存和cursor导航；
- `showAiDecisionFeedback`等业务字段是否正确；
- AI后端联调地址和测试Token；
- 文件预览接口返回内容和短时URL有效期；
- 后端业务错误码和`requestId`日志定位。

只有“AI后端如何经客户网关暴露给浏览器”需要提前问一体化或部署人员，接口业务本身继续和公司后端确认。

## 五、23号结束前至少拿到的结果

- [ ] 一体化正确仓库和目标分支，可在本地安装、启动和构建。
- [ ] AI模块目标目录、两个空白路由的接入方式和参考代码。
- [ ] 公共Axios、Token处理、登录失效和统一响应处理的参考文件。
- [ ] 必须替换的公共组件范围，尤其是抽屉和文件预览。
- [ ] ECP Loader负责人、托管方式、四类入口和参数映射负责人。
- [ ] ECP与一体化各环境Origin、iframe/CSP配置负责人。
- [ ] AI接口网关前缀及开发代理/生产转发负责人。
- [ ] 后续迁入、评审、联合验证和上线的流程及联系人。

> 若当天无法确定某项，不要只写“后续确认”，应记录具体负责人和最晚答复时间。23号的目标是消除迁入方式的不确定性，而不是开始复制代码。
