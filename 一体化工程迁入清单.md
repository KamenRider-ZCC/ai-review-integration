# AI评标前端迁入一体化工程清单

> 用途：23号取得客户一体化工程代码后，按本清单核对技术栈、迁移业务模块并替换客户公共组件。不要直接覆盖客户工程入口、路由、请求层或全局样式。

## 一、迁移原则

1. 迁移AI评标业务代码，不搬整套独立Vue工程。
2. 优先复用客户已有的Router、Axios实例、认证工具、Ant Design注册方式和公共组件。
3. 六个后端接口、入口参数、Loader通信消息和业务判断规则保持不变。
4. 先保证真实接口流程跑通，再替换外观类公共组件；每替换一类组件立即回归。
5. `Access-Token`不通过URL、请求体或`postMessage`传递，也不能写入日志。

## 二、文件迁移范围

### 2.1 必须迁入的业务文件

| 当前文件 | 作用 | 迁入方式 |
| --- | --- | --- |
| `src/pages/ReviewProgressPage.vue` | AI评审进度、轮询、失败文件重试、展开/收起 | 迁入客户业务页面目录 |
| `src/pages/ReviewPage.vue` | 判定/评分结果、全部不一致项、反馈保存、导航 | 迁入客户业务页面目录 |
| `src/types/review.ts` | 六个接口和业务数据类型 | 迁入客户类型目录，字段不得自行改名 |
| `src/types/entry.ts` | 三类页面入口类型 | 迁入客户类型目录 |
| `src/utils/entry.ts` | URL参数读取和入口校验 | 直接迁入或改接客户路由类型 |
| `src/api/review.ts` | 六个AI评标接口 | 保留接口函数，接入客户请求实例 |
| `src/bridge/hostBridge.ts` | AI页面与Loader的关闭、尺寸、错误通信 | 继续使用iframe时必须迁入 |

### 2.2 迁入后可能被客户公共能力替换

| 当前文件 | 当前作用 | 驻场处理 |
| --- | --- | --- |
| `src/api/http.ts` | Axios实例、Token、请求ID、响应包络和错误转换 | 优先改接客户公共请求实例和拦截器 |
| `src/utils/cookie.ts` | 从当前域读取`Access-Token` | 优先改用客户认证工具；确认Cookie可读方式 |
| `src/components/PreviewPanel.vue` | 投标文件原文预览外壳 | 优先替换为客户公共文件预览组件 |
| `src/components/SectionTitle.vue` | 抽屉区块标题 | 有同类公共标题组件则替换，否则保留 |
| `src/styles/global.css` | 独立工程主题变量和基础样式 | 只迁移`--ai-*`变量或映射客户主题，不能整体覆盖全局样式 |

### 2.3 只提取配置，不直接覆盖客户文件

| 当前文件 | 需要提取的内容 | 禁止操作 |
| --- | --- | --- |
| `src/router/index.ts` | `/ai-review/progress`和`/ai-review/page`两条路由 | 不替换客户Router实例和404路由 |
| `src/main.ts` | Ant Design样式依赖情况 | 不重新`createApp`，不重复注册Router、Pinia或Antd |
| `src/App.vue` | 无需迁移 | 不覆盖客户根组件 |
| `src/env.d.ts` | AI评标环境变量类型 | 合并到客户已有环境声明 |
| `vite.config.ts` | 本地代理规则和路径别名 | 合并配置，不覆盖客户Vite配置 |
| `package.json` | 依赖版本参考 | 不覆盖客户依赖文件和脚本 |

### 2.4 仅用于公司内开发，不迁入生产工程

- `src/mocks/review.ts`
- `public/ecp-simulator.html`
- `public/mock-document.html`
- `.env.example`中的本地Mock配置
- `README.md`
- `2026-09-21联调清单.md`
- `dist/`、`node_modules/`、`pnpm-lock.yaml`

### 2.5 Loader单独交付宿主侧

`public/ai-review-loader.js`不是一体化业务页面组件，而是ECP宿主侧加载AI页面的接入脚本，应单独交给具备ECP权限的客户开发人员。

宿主侧需要完成：

- 引入`ai-review-loader.js`；
- 使用AI页面真实地址配置`AIReview.init({ appBaseUrl })`；
- 在评审资料、评审矩阵和评审汇总的对应入口调用`AIReview.open`；
- 传递`reviewId、action`及必要的矩阵坐标；
- 处理`onOpened、onReady、onClosed、onError`回调；
- 不通过Loader传Token、专家结果、AI结果或文件内容。

## 三、客户工程依赖核对

当前独立工程依赖：

| 依赖 | 当前版本 | 实际使用情况 | 迁入建议 |
| --- | --- | --- | --- |
| Vue | 3.4 | 页面和组件基础 | 使用客户现有Vue，不重复安装 |
| Vue Router | 4.4 | 路由和Query读取 | 合并两条业务路由 |
| Ant Design Vue | 3.2 | 抽屉、按钮、提示、选择框等 | 先确认客户版本和全局注册方式 |
| Axios | 1.7 | 六个接口请求 | 优先使用客户公共Axios实例 |
| `@ant-design/icons-vue` | 6.1 | 进度页两个图标 | 客户已有则复用，没有可改用公共图标 |
| Pinia | 2.1 | 业务代码未使用，仅在独立入口初始化 | 本模块不要求引入Pinia |

必须确认客户版本是否存在破坏性差异，特别是：

- Ant Design Vue抽屉属性使用`visible`还是`open`；
- Vue Router的路由前缀、history模式和权限守卫；
- Axios公共实例是否自动加Token、处理业务码和登录失效；
- 客户是否已全局注册Ant Design组件和样式；
- TypeScript、ESLint和路径别名规则。

## 四、公共组件和基础能力替换表

| 当前实现 | 客户侧优先核对 | 替换时必须保持的行为 |
| --- | --- | --- |
| `<a-drawer>` | 公共抽屉组件 | 560px、遮罩、正文滚动、反馈/专家结果/底栏固定、关闭回调 |
| `<a-button>` | 公共按钮 | 禁用、加载、主次按钮状态和链接按钮样式 |
| `<a-checkbox>`、`<a-checkbox-group>` | 公共选择组件 | 多选、只读、反馈项勾选后自动展开 |
| `<a-alert>` | 公共提示组件 | 加载失败、业务错误和只读提示 |
| `<a-spin>`、`<a-empty>` | 公共加载和空状态 | 首次加载、预览加载、无不一致项 |
| `message.success/error` | 公共消息工具 | 重试受理、反馈保存和接口错误提示 |
| `PreviewPanel.vue` | 公共PDF/文件预览 | 短时URL、上一处/下一处、关闭、文件名和引用定位 |
| `SectionTitle.vue` | 公共区块标题 | 蓝色左边线、标题层级和间距 |
| `api/http.ts` | 公共Axios实例 | 原始`Access-Token`、`X-Request-Id`、20秒超时、统一包络 |
| `cookie.ts` | 公共认证工具 | AI页面能取得当前登录Token，不能从父iframe读取跨域Cookie |
| `hostBridge.ts` | 客户iframe通信封装 | 来源白名单、关闭、进度宽度、错误回传 |
| `global.css`主题变量 | 客户主题Token | 主色、成功/警告/错误色、边框、文本层级一致 |

组件替换顺序建议：

1. 请求层和Token读取；
2. 路由和Loader通信；
3. 文件预览；
4. 抽屉、按钮、提示等通用UI；
5. 主题变量和细节样式。

不要一次替换所有组件后再测试，否则无法快速判断是哪一层引入的问题。

## 五、23号拿到客户代码后先核对

### 5.1 工程结构

- [ ] Vue、Vue Router、Ant Design Vue、Axios和TypeScript版本
- [ ] 源码目录规范、路径别名和组件命名规范
- [ ] 路由表位置、路由前缀、history模式和权限守卫
- [ ] 是否使用微前端、iframe或其他子应用加载方式
- [ ] 静态资源发布路径和`publicPath/base`配置
- [ ] 开发、测试、生产环境变量命名规范

### 5.2 请求和认证

- [ ] 公共Axios实例位置及调用方式
- [ ] 公共响应是否已自动拆包，是否仍返回`success/code/message/requestId/data`
- [ ] Token由Cookie、状态管理还是请求拦截器提供
- [ ] Cookie名称是否仍为`Access-Token`
- [ ] Cookie是否为`HttpOnly`，Domain和Path是否覆盖AI页面
- [ ] 公共请求层是否会自动添加`Bearer`；本接口要求传原始Token
- [ ] 登录失效、业务错误、超时和网络错误的统一处理方式
- [ ] 测试环境代理及生产网关如何配置`/ai-review-api/v1`

### 5.3 公共组件

- [ ] 公共抽屉是否支持自定义头部、固定底栏和正文滚动
- [ ] 公共文件预览是否支持短时URL及引用位置切换
- [ ] 公共按钮、选择框、提示、加载、空状态和消息组件API
- [ ] 公共主题变量、字号、间距、z-index和遮罩规范
- [ ] 文件预览与抽屉并排时是否存在层级冲突

### 5.4 iframe和安全策略

- [ ] ECP宿主地址和AI页面地址是否跨域
- [ ] `VITE_ALLOWED_PARENT_ORIGINS`应配置哪些真实Origin
- [ ] 页面是否允许被ECP通过iframe加载
- [ ] CSP `frame-ancestors`和`X-Frame-Options`是否放行宿主
- [ ] 预览地址是否允许在AI页面iframe内再次嵌套加载
- [ ] Loader脚本由哪个工程托管、如何发布和缓存更新

## 六、推荐迁移步骤

1. 在客户工程建立AI评标业务目录，不改客户现有入口。
2. 迁入`types`、`utils/entry.ts`和六个接口函数。
3. 用客户公共Axios实例改写`api/http.ts`接入点。
4. 确认Token读取方式并替换`cookie.ts`，不要写死Token。
5. 迁入两个页面和必要组件，先保留现有UI实现。
6. 把两条AI评标路由合并到客户路由表并通过权限守卫。
7. 迁入`hostBridge.ts`，配置真实父页面Origin。
8. 在ECP或宿主测试页接入Loader，先跑通三个页面入口。
9. 关闭Mock，依照`2026-09-21联调清单.md`跑通六个真实接口。
10. 按第三方公共组件替换表逐项替换并回归。
11. 合并主题样式，检查抽屉、进度浮窗和文件预览的层级。
12. 执行客户工程的类型检查、Lint、构建和回归测试。

## 七、迁移后必须保持的业务约束

- 页面入口只支持`progress`和`page`。
- `action`只支持`AI`和`AI_FEEDBACK`。
- `reviewStage、reviewItemId、bidderId`必须同时存在或同时不存在。
- 无矩阵坐标时只允许`action=AI_FEEDBACK`。
- 是否显示反馈区以后端`showAiDecisionFeedback`为准。
- 普通矩阵导航使用后端返回的矩阵坐标。
- 全部不一致项导航使用后端返回的cursor。
- 轮询开关和间隔以后端`pollingRequired、pollingIntervalSeconds`为准。
- 只有`retryable=true`的失败文件允许重新解析。
- 问题类型使用后端返回的选项，选择“其他”时说明必填。
- Token、预览地址和敏感文件内容不得写日志。

## 八、迁移后验收

- [ ] 进度页可展开、收起、轮询并重新解析失败文件
- [ ] 形式、资格、响应性评审能显示判定结果
- [ ] 详细评审能显示字符串分数和主客观类型
- [ ] 不一致时显示反馈，一致时不显示
- [ ] 全部不一致项能按专家权限查看和编辑
- [ ] 保存反馈后正确回显并更新完成数量
- [ ] 文件预览和上一处/下一处正常
- [ ] 抽屉关闭后宿主滚动恢复，iframe被销毁
- [ ] 非法入口参数在请求前被拦截
- [ ] 业务错误保留`code、message、requestId`
- [ ] 浏览器控制台无跨域、CSP、Vue或组件警告
- [ ] 客户工程类型检查、Lint和构建全部通过

## 九、23号需要形成的结论

当天不要只记录“看过客户代码”，应明确产出以下结论：

| 决策项 | 结论 |
| --- | --- |
| AI业务代码最终目录 |  |
| 两条业务路由最终地址 |  |
| 公共Axios实例及响应拆包方式 |  |
| Token实际读取方式 |  |
| 后端地址和代理/网关配置 |  |
| 抽屉是否替换 |  |
| 文件预览是否替换 |  |
| 其他公共组件替换范围 |  |
| Loader托管工程和发布方式 |  |
| 允许的父页面Origin |  |
| CSP/iframe配置负责人 |  |
| 首次迁移和联调时间 |  |

